---
  version: "1.0"
  steps:
    test-step:
      fail-fast: false
      image: "golang:alpine"
      working-directory: ${{initial-clone}}
      commands:
        - "/usr/local/go/bin/go test -v --cover"


    security:
      fail-fast: false
      image: "node:latest"
      working-directory: ${{initial-clone}}
      commands:
        - "echo not implemented"


    build-docker-image:
             type: "build"
             fail-fast: false
             dockerfile: "Dockerfile"
             image-name: "ironsource/enrichment-go"
             tag: latest
