version: '1.0'
kind: pipeline
metadata:
  name: pipeline-crud/crud
  project: pipeline-crud
  originalYamlString: |
    version: '1.0'
    steps:
      test:
        image: alpine
        commands:
          - echo test
spec:
  triggers: []
  stages: []
  variables: []
  contexts: []
  steps:
    test:
      image: alpine
      commands:
        - echo test

