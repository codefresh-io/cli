---
  version: "1.0"
  steps: 
    - 
      mystep: 
        image: "node"
        fail-step: true
    - 
      build-step: 
        type: "build"
        image: "node"
        fail-step: true
        dockerfile: "Dockerfile"
        fail-fast: false
        image-name: "owner/imageName"
        tag: "latest"
