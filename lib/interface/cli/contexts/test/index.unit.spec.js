'use strict';

const context = require('../index');

describe('Contexts tests', () => {

    describe('loadConfigFile', () => {

        describe('positive', () => {

            test('adds 1 + 2 to equal 3', () => {
                expect(3).toBe(3);
            });

        });

        describe('negative', () => {

        });

    });

});

