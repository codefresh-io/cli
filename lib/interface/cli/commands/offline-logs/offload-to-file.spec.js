const {objectIdFromDate, writeLogsToFile, checkRemnant} = require('./utils')

descrbie('offload-to-file', () => {
  describe('writeLogsToFile', () => {
    it('creates a correct file when blah blah blah', () => {
      writeLogsToFile()
    });
  });
});
