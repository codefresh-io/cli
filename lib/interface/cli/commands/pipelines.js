const yargs = require('yargs');
const assert = require('assert');
const _  = require('lodash');
const top = require('./top/index');


class Pipelines{
  create()
  {
    top.

  }
  run();
  delete()
}
module.exports = pipelines;
