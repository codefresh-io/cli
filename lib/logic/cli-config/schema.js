/**
 * Use for future properties definition.
 *
 * key -- property name
 * value -- default value form Model.default()
 *
 * todo: add validation on schema
 * */
const schema = {
    output: {
        pretty: false,
    },
};

module.exports = schema;
